import { SearchPipeHomePipe } from './search-pipe-home.pipe';

describe('SearchPipeHomePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPipeHomePipe();
    expect(pipe).toBeTruthy();
  });
});
